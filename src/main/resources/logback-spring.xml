<configuration>
    <springProperty scope="context" name="application_name" source="spring.application.name"/>

    <appender name="jsonConsoleAppender" class="ch.qos.logback.core.ConsoleAppender">
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>DEBUG</level>
            <onMatch>NEUTRAL</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <customFields>{"app_name":"${application_name}"}</customFields>
            <fieldNames>
                <timestamp>@timestamp</timestamp>
                <level>level</level>
                <logger>logger_name</logger>
                <thread>thread_name</thread>
                <message>message</message>
            </fieldNames>
            <!-- OpenTelemetry Micrometer Format -->
            <includeMdcKeyName>traceId</includeMdcKeyName>
            <includeMdcKeyName>spanId</includeMdcKeyName>
            <!-- OpenTelemetry Format -->
            <includeMdcKeyName>trace_id</includeMdcKeyName>
            <includeMdcKeyName>span_id</includeMdcKeyName>
            <!-- ECS APM Telemetry Format -->
            <includeMdcKeyName>trace.id</includeMdcKeyName>
            <includeMdcKeyName>transcation.id</includeMdcKeyName>
            <includeMdcKeyName>span.id</includeMdcKeyName>
        </encoder>
    </appender>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <!-- Trace-/Span-IDs aus MDC: OTel: traceId/spanId -->
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level ${application_name} %logger - [%X{trace_id}-%X{span_id}] %msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="jsonConsoleAppender" />
    </root>
</configuration>